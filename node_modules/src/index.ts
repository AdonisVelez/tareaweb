import { mostrarPersonas, mostrarEncuestas, mostrarRegistros, buscarRegistroPorID } from './funciones';

// Llamar a las funciones para mostrar información
mostrarPersonas();
mostrarEncuestas();
mostrarRegistros();

// Llamar a la función para buscar registro por ID
const registroEncontrado = buscarRegistroPorID(3);
if (registroEncontrado) {
  console.log("Registro encontrado:");
  console.log(registroEncontrado);
} else {
  console.log("Registro no encontrado");
}
